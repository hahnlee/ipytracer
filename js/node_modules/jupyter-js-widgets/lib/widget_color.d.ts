import { LabeledDOMWidgetModel, LabeledDOMWidgetView } from './widget';
export declare class ColorPickerModel extends LabeledDOMWidgetModel {
    defaults(): any;
}
export declare class ColorPickerView extends LabeledDOMWidgetView {
    render(): void;
    events(): {
        [e: string]: string;
    };
    private _update_value();
    private _update_concise();
    private _picker_change();
    private _text_change();
    private _validate_color(color, fallback);
    private _color_container;
    private _textbox;
    private _colorpicker;
}

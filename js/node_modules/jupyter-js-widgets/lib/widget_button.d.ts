import { DOMWidgetModel, DOMWidgetView } from './widget';
export declare class ButtonModel extends DOMWidgetModel {
    defaults(): any;
}
export declare class ButtonView extends DOMWidgetView {
    /**
     * Called when view is rendered.
     */
    render(): void;
    /**
     * Update the contents of this view
     *
     * Called when the model is changed. The model may have been
     * changed by another view or by a state update from the back-end.
     */
    update(): void;
    update_button_style(): void;
    /**
     * Dictionary of events and handlers
     */
    events(): {
        [e: string]: string;
    };
    /**
     * Handles when the button is clicked.
     */
    _handle_click(event: any): void;
    /**
     * The default tag name.
     *
     * #### Notes
     * This is a read-only attribute.
     */
    readonly tagName: string;
    el: HTMLButtonElement;
}

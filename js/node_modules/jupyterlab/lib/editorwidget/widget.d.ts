import 'codemirror/mode/meta';
import { Token } from 'phosphor/lib/core/token';
import { IInstanceTracker } from '../common/instancetracker';
import { CodeMirrorWidget } from '../codemirror/widget';
import { ABCWidgetFactory, DocumentRegistry } from '../docregistry';
/**
 * A class that tracks editor widgets.
 */
export interface IEditorTracker extends IInstanceTracker<EditorWidget> {
}
/**
 * The editor tracker token.
 */
export declare const IEditorTracker: Token<IEditorTracker>;
/**
 * A document widget for codemirrors.
 */
export declare class EditorWidget extends CodeMirrorWidget {
    /**
     * Construct a new editor widget.
     */
    constructor(context: DocumentRegistry.IContext<DocumentRegistry.IModel>);
    /**
     * Get the context for the editor widget.
     */
    readonly context: DocumentRegistry.IContext<DocumentRegistry.IModel>;
    private _context;
}
/**
 * A widget factory for editors.
 */
export declare class EditorWidgetFactory extends ABCWidgetFactory<EditorWidget, DocumentRegistry.IModel> {
    /**
     * Create a new widget given a context.
     */
    protected createNewWidget(context: DocumentRegistry.IContext<DocumentRegistry.IModel>): EditorWidget;
}

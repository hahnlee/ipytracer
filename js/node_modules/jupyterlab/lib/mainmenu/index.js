// Copyright (c) Jupyter Development Team.
// Distributed under the terms of the Modified BSD License.
"use strict";
var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
var searching_1 = require('phosphor/lib/algorithm/searching');
var vector_1 = require('phosphor/lib/collections/vector');
var token_1 = require('phosphor/lib/core/token');
var menubar_1 = require('phosphor/lib/ui/menubar');
/* tslint:disable */
/**
 * The main menu token.
 */
exports.IMainMenu = new token_1.Token('jupyter.services.main-menu');
/**
 * The main menu class.  It is intended to be used as a singleton.
 */
var MainMenu = (function (_super) {
    __extends(MainMenu, _super);
    function MainMenu() {
        _super.apply(this, arguments);
        this._items = new vector_1.Vector();
    }
    /**
     * Add a new menu to the main menu bar.
     */
    MainMenu.prototype.addMenu = function (menu, options) {
        var _this = this;
        if (options === void 0) { options = {}; }
        if (searching_1.indexOf(this.menus, menu) > -1) {
            return;
        }
        var rank = 'rank' in options ? options.rank : 100;
        var rankItem = { menu: menu, rank: rank };
        var index = searching_1.upperBound(this._items, rankItem, Private.itemCmp);
        // Upon disposal, remove the menu and its rank reference.
        menu.disposed.connect(function () {
            _this.removeMenu(menu);
            _this._items.remove(rankItem);
        });
        this._items.insert(index, rankItem);
        this.insertMenu(index, menu);
    };
    return MainMenu;
}(menubar_1.MenuBar));
exports.MainMenu = MainMenu;
/**
 * A namespace for private data.
 */
var Private;
(function (Private) {
    /**
     * A comparator function for menu rank items.
     */
    function itemCmp(first, second) {
        return first.rank - second.rank;
    }
    Private.itemCmp = itemCmp;
})(Private || (Private = {}));
//# sourceMappingURL=index.js.map